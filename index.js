// index.js

/**
 * Sample function to be included in your npm package.
 * @param {string} name - The name to greet.
 * @returns {string} - The greeting message.
 */
function greet(name) {
  return `Hello, ${name}! Welcome to npmteazaty!`;
}

module.exports = {
  greet,
};
